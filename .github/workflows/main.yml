name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Build LaTeX
        uses: xu-cheng/latex-action@master
        with:
          root_file: index.tex
